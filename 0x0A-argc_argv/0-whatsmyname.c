#include <stdio.h>

/**
 * main - program that prints its name, followed by a new line
 * @argc: argument that counts argument input
 * @argv: stores thestrings in an array of char* (strings)
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
