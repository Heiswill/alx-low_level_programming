#include <stdio.h>

/**
 * main - print single digit of numbers of base 10(0 - 9)
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
